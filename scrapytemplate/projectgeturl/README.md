run the command below inside the folder where seed.txt exist to start the crawler

nohup scrapy crawl example &

You might want to modify the user agent / download delay in settings.py if you want. 

